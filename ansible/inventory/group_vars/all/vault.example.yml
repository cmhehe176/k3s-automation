---
# ==============================================================================
# 🔐 ANSIBLE VAULT / CREDENTIALS MANAGEMENT (TEMPLATE / EXAMPLE)
# ==============================================================================
# Hướng dẫn sử dụng:
#   1. Copy file này thành vault.yml:
#      cp ansible/inventory/group_vars/all/vault.example.yml ansible/inventory/group_vars/all/vault.yml
#
#   2. Thay đổi các mật khẩu / secrets theo ý bạn hoặc dùng biến môi trường.
#
#   3. (Tùy chọn) Mã hóa file vault.yml bằng Ansible Vault:
#      ansible-vault encrypt ansible/inventory/group_vars/all/vault.yml
# ==============================================================================

# --- K3s Cluster Token ---
k3s_token: "K3sSecretClusterToken2026SecureKey"

# --- ArgoCD Admin Credentials ---
argocd_admin_user: "admin"
argocd_admin_password: "Admin@2026"

# --- KubeSphere Console Credentials ---
kubesphere_admin_user: "admin"
kubesphere_admin_password: "P@88w0rd"

# --- Oracle Database XE Credentials ---
oracle_sys_password: "Oracle@2026"
oracle_app_user: "app_user"
oracle_app_password: "App@2026"

# --- Redis Cluster Credentials ---
redis_password: "Redis@2026"

# --- MinIO S3 Object Storage Credentials ---
minio_root_user: "minioadmin"
minio_root_password: "Minio@2026"

# --- Red Hat OpenShift Web Console & Dex OIDC Keys ---
console_client_secret: "OpenShiftConsoleSecretKey2026"
console_cookie_encryption_key: "k3sConsoleCookieEncryptKey32Byte"
console_cookie_auth_key: "k3sConsoleCookieAuthKey64ByteLongRandomStringForSessionValidation12"

# --- OpenShift Console Dex OIDC Users ---
console_users:
  - username: "superadmin"
    email: "superadmin@mbfs.vn"
    password: "Admin@2026"
    groups:
      - "superadmins"

  - username: "admin"
    email: "admin@mbfs.vn"
    password: "Hihi@1234"
    groups:
      - "admins"

  - username: "developer"
    email: "dev@mbfs.pro"
    password: "Hihi@1234"
    groups:
      - "developers"
